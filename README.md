# reps

Chat first training log. Talk in t3code, agent stores via `log.py` into local SQLite.

See AGENTS.md for the agent protocol. Dashboard lives in `dashboard/` as a read only Cloudflare Worker over a pushed snapshot.
